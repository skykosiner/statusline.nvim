local defaults = {
    GitInfo = false,
    FileInfo = false,
    ModeInfo = false,
    LineInfo = false,
    LspInfo = false,
    -- I'm not too suer if I want this on tbh
    SaveInfo = false,
    backgroundColor = "#7fa3c0",
}

return defaults
